import { PipesPruebaPipe } from './pipes-prueba.pipe';

describe('PipesPruebaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipesPruebaPipe();
    expect(pipe).toBeTruthy();
  });
});
